[package]
name = "chronicle-core"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Deterministic UTC cron occurrence evaluator"

[dependencies]
chrono = "0.4"
chrono-tz = "0.10"

[dev-dependencies]
proptest = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
